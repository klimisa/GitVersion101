name: pr-$(System.PullRequest.PullRequestNumber)$(Rev:.r)

trigger:
  batch: false
  branches:
    exclude:
      - master
  paths:
    exclude:
      - README.md

jobs:
  - template: azure-pipelines-common.yml