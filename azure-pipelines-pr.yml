name: 0.$(System.PullRequest.PullRequestNumber)$(Rev:.r)-pr

trigger:
  batch: false
  branches:
    exclude:
      - master
  paths:
    exclude:
      - README.md

jobs:
  - template: azure-pipelines-common.yml